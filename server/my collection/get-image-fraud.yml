info:
  name: get-image-fraud
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:5000/api/visits/images/{{imageId}}/fraud
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
