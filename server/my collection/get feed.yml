info:
  name: get feed
  type: http

http:
  method: GET
  url: http://localhost:5000/api/dashboard/feed
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
