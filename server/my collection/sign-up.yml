info:
  name: sign-up
  type: http

http:
  method: POST
  url: http://localhost:5000/api/auth/sign-up/email
  body:
    type: json
    data: |-
      {
        "name": "John Doe",
        "email": "johnassex@ample.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​