info:
  name: past visits
  type: http

http:
  method: GET
  url: http://localhost:5000/api/visits/rep/507f1f77bcf86cd799439012
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​