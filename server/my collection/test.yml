info:
  name: Test
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5000/api/test
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
